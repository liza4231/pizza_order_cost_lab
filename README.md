This program calculates the total cost of pizza based on size, number of topping and delivery distance.
If any input is invalid an error message prints and exits.

if all the inputs are valid the total cost is calculated and displayed using an f'string
